# 粉末类物品配方修复指南

本指南提供了具体的JSON文件位置和修复方法。

---

## 🔴 优先级1: 砂叶粉末 (ID: 543) - 8个配方缺设备

**文件位置**: `data/synthesis_tables/543.json`

### 问题描述
砂叶粉末的合成表中有8个配方缺少设备信息。这些配方应该使用**研磨机** (ID: 54)。

### 修复方法

在 `data/synthesis_tables/543.json` 中，找到以下配方并添加设备ID:

#### 配方1: 高晶粉末
```json
{
  "device_id": "54",  // 添加这一行
  "materials": [
    {"id": "29", "count": "2"},   // 紫晶粉末
    {"id": "543", "count": "1"}   // 砂叶粉末
  ],
  "products": [
    {"id": "546", "count": "1"}   // 高晶粉末
  ]
}
```

#### 配方2: 致密源石粉末
```json
{
  "device_id": "54",  // 添加这一行
  "materials": [
    {"id": "47", "count": "2"},   // 源石粉末
    {"id": "543", "count": "1"}   // 砂叶粉末
  ],
  "products": [
    {"id": "544", "count": "1"}   // 致密源石粉末
  ]
}
```

#### 配方3: 致密碳粉末
```json
{
  "device_id": "54",  // 添加这一行
  "materials": [
    {"id": "195", "count": "2"},  // 碳粉末
    {"id": "543", "count": "1"}   // 砂叶粉末
  ],
  "products": [
    {"id": "548", "count": "1"}   // 致密碳粉末
  ]
}
```

#### 配方4: 致密晶体粉末
```json
{
  "device_id": "54",  // 添加这一行
  "materials": [
    {"id": "38", "count": "2"},   // 晶体外壳粉末
    {"id": "543", "count": "1"}   // 砂叶粉末
  ],
  "products": [
    {"id": "545", "count": "1"}   // 致密晶体粉末
  ]
}
```

#### 配方5: 细磨荞花粉末
```json
{
  "device_id": "54",  // 添加这一行
  "materials": [
    {"id": "379", "count": "2"},  // 荞花粉末
    {"id": "543", "count": "1"}   // 砂叶粉末
  ],
  "products": [
    {"id": "553", "count": "1"}   // 细磨荞花粉末
  ]
}
```

#### 配方6: 细磨柑实粉末
```json
{
  "device_id": "54",  // 添加这一行
  "materials": [
    {"id": "380", "count": "2"},  // 柑实粉末
    {"id": "543", "count": "1"}   // 砂叶粉末
  ],
  "products": [
    {"id": "554", "count": "1"}   // 细磨柑实粉末
  ]
}
```

#### 配方7: 致密蓝铁粉末
```json
{
  "device_id": "54",  // 添加这一行
  "materials": [
    {"id": "368", "count": "2"},  // 蓝铁粉末
    {"id": "543", "count": "1"}   // 砂叶粉末
  ],
  "products": [
    {"id": "547", "count": "1"}   // 致密蓝铁粉末
  ]
}
```

---

## 🟡 优先级2: 碳粉末 (ID: 195) - 1个配方缺设备

**文件位置**: `data/synthesis_tables/195.json`

### 问题描述
碳粉末有一个特殊的混合配方缺少设备。

### 修复方法

找到以下配方并添加设备ID（需要在游戏中验证正确的设备）:

```json
{
  "device_id": "53",  // 可能是精炼炉(53)，需要验证
  "materials": [
    {"id": "379", "count": "1"},  // 荞花粉末
    {"id": "380", "count": "1"},  // 柑实粉末
    {"id": "587", "count": "1"},  // 芽针粉末
    {"id": "586", "count": "1"}   // 锦草粉末
  ],
  "products": [
    {"id": "195", "count": "1"}   // 碳粉末
  ]
}
```

**需要验证**: 这个配方的正确设备ID

---

## 🟡 优先级2: 致密晶体粉末 (ID: 545) - 1个配方缺设备

**文件位置**: `data/synthesis_tables/545.json`

### 问题描述
致密晶体粉末有一个配方缺少设备。

### 修复方法

找到以下配方并添加设备ID（需要在游戏中验证）:

```json
{
  "device_id": "?",  // 需要在游戏中查找正确的设备ID
  "materials": [
    {"id": "545", "count": "1"}   // 致密晶体粉末
  ],
  "products": [
    {"id": "557", "count": "1"}   // 密制晶体
  ]
}
```

**需要验证**: 这个配方的正确设备ID

---

## 🟡 优先级2: 致密源石粉末 (ID: 544) - 1个配方缺设备

**文件位置**: `data/synthesis_tables/544.json`

### 问题描述
致密源石粉末有一个配方缺少设备。

### 修复方法

找到以下配方并添加设备ID（可能是封装机）:

```json
{
  "device_id": "56",  // 可能是封装机(56)，需要验证
  "materials": [
    {"id": "544", "count": "15"},  // 致密源石粉末
    {"id": "771", "count": "5"}    // 息壤
  ],
  "products": [
    {"id": "767", "count": "1"}    // 低容武陵电池
  ]
}
```

**需要验证**: 这个配方的正确设备ID

---

## 🟡 优先级2: 源石粉末 (ID: 47) - 1个配方缺设备

**文件位置**: `data/synthesis_tables/47.json`

### 问题描述
源石粉末有一个配方缺少设备。

### 修复方法

找到以下配方并添加设备ID（可能是封装机）:

```json
{
  "device_id": "56",  // 可能是封装机(56)，需要验证
  "materials": [
    {"id": "47", "count": "15"},   // 源石粉末
    {"id": "373", "count": "10"}   // 铁制零件
  ],
  "products": [
    {"id": "378", "count": "1"}    // 中容谷地电池
  ]
}
```

**需要验证**: 这个配方的正确设备ID

---

## 🟡 优先级3: 紫晶粉末 (ID: 29) - 纤维作为原料

**文件位置**: `data/synthesis_tables/29.json`

### 问题描述
紫晶粉末的配方使用纤维作为原料，这看起来不合理。

### 当前配方
```json
{
  "device_id": "54",  // 粉碎机
  "materials": [
    {"id": "193", "count": "1"}   // 紫晶纤维
  ],
  "products": [
    {"id": "29", "count": "1"}    // 紫晶粉末
  ]
}
```

### 可能的修复方案

**方案A**: 原料应该是紫晶矿而不是纤维
```json
{
  "device_id": "54",  // 粉碎机
  "materials": [
    {"id": "49", "count": "1"}    // 紫晶矿（而不是193）
  ],
  "products": [
    {"id": "29", "count": "1"}    // 紫晶粉末
  ]
}
```

**方案B**: 设备应该是其他而不是粉碎机
```json
{
  "device_id": "?",  // 需要验证正确的设备
  "materials": [
    {"id": "193", "count": "1"}   // 紫晶纤维
  ],
  "products": [
    {"id": "29", "count": "1"}    // 紫晶粉末
  ]
}
```

**需要验证**: 在游戏中确认正确的原料和设备

---

## 📋 修复检查清单

### 修复前
- [ ] 备份原始JSON文件
- [ ] 在游戏中验证所有配方
- [ ] 记录正确的设备ID

### 修复中
- [ ] 修改 `data/synthesis_tables/543.json` (8个配方)
- [ ] 修改 `data/synthesis_tables/195.json` (1个配方)
- [ ] 修改 `data/synthesis_tables/545.json` (1个配方)
- [ ] 修改 `data/synthesis_tables/544.json` (1个配方)
- [ ] 修改 `data/synthesis_tables/47.json` (1个配方)
- [ ] 修改 `data/synthesis_tables/29.json` (1个配方)

### 修复后
- [ ] 运行验证脚本检查JSON格式
- [ ] 重新生成设备生产表 (`extract_device_productions.py`)
- [ ] 更新分析报告
- [ ] 提交git commit

---

## 🔍 验证方法

修复后，运行以下命令验证:

```bash
# 验证JSON格式
python3 -m json.tool data/synthesis_tables/543.json > /dev/null && echo "✓ 543.json 格式正确"

# 重新生成设备生产表
python3 data/extract_device_productions.py

# 运行分析脚本
python3 << 'VERIFY'
import json
import os

with open('data/synthesis_tables/543.json', 'r', encoding='utf-8') as f:
    data = json.load(f)

# 检查是否所有配方都有设备
for table in data.get('tables', []):
    for row in table.get('data', []):
        if len(row) > 0 and row[0]:
            print(f"✓ 配方有设备")
        else:
            print(f"✗ 配方缺少设备")
VERIFY
```

---

## 📞 需要帮助?

如果修复过程中遇到问题:

1. 检查JSON格式是否正确 (使用 `python3 -m json.tool`)
2. 验证设备ID是否存在于 `data/type5_devices.json`
3. 验证物品ID是否存在于 `data/type6_items.json`
4. 查看原始的 `data/item_details/` 文件确认配方信息

